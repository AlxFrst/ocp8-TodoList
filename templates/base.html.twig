<!DOCTYPE html>
<html lang="fr">
	<head>
		<meta charset="utf-8">
		<meta name="viewport" content="width=device-width, initial-scale=1">
		<title>To Do List app</title>
		<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
		<link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
		<style>
			body {
				background-color: #f5f5f7;
				font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen-Sans, Ubuntu, Cantarell, 'Helvetica Neue', sans-serif;
			}
			.navbar {
				background-color: rgba(255, 255, 255, 0.8);
				backdrop-filter: saturate(180%) blur(20px);
				box-shadow: 0 1px 0 rgba(0, 0, 0, 0.1);
			}
			.navbar-brand img {
				margin-right: 10px;
			}
			.btn-custom {
				border-radius: 980px;
				padding: 8px 16px;
				font-size: 14px;
				font-weight: 400;
				transition: all 0.3s;
				background-color: #0071e3;
				color: #ffffff;
				border: none;
			}
			.btn-custom:hover {
				background-color: #0077ed;
				color: #ffffff;
				transform: translateY(-2px);
				box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
			}
			.alert {
				border-radius: 12px;
				border: none;
			}
			.footer {
				background-color: #f5f5f7;
				border-top: none;
			}
			.bg-white {
				background-color: #ffffff;
				border-radius: 12px;
				box-shadow: 0 4px 6px rgba(0, 0, 0, 0.05);
			}
			.dropdown-menu {
				border-radius: 12px;
				box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
				border: none;
			}
			.dropdown-item {
				transition: all 0.2s;
			}
			.dropdown-item:hover {
				background-color: #f5f5f7;
			}
			.btn-action {
				background-color: #f5f5f7;
				color: #0071e3;
				border: none;
				border-radius: 980px;
				padding: 10px 20px;
				font-size: 16px;
				font-weight: 500;
				transition: all 0.3s ease;
				text-decoration: none;
				display: inline-block;
				margin: 5px;
			}
			.btn-action:hover {
				background-color: #0071e3;
				color: #ffffff;
				transform: translateY(-2px);
				box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
			}
			.btn-action i {
				margin-right: 8px;
			}
		</style>
	</head>

	<body class="d-flex flex-column min-vh-100">
		<nav class="navbar navbar-expand-lg navbar-light">
			<div class="container">
				<a class="navbar-brand d-flex align-items-center" href="{{ path('homepage') }}">
					<img src="{{ asset('img/Logo_OpenClassrooms.png') }}" alt="OpenClassrooms" height="40" class="d-inline-block align-top">
					<span class="ms-2 fw-bold">To Do List app</span>
				</a>
				<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
					<span class="navbar-toggler-icon"></span>
				</button>
				<div class="collapse navbar-collapse justify-content-end" id="navbarNav">
					<ul class="navbar-nav">
						{% if is_granted('ROLE_ADMIN') %}
							<li class="nav-item dropdown">
								<a class="nav-link dropdown-toggle btn-custom me-2" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
									<i class="fas fa-users me-2"></i>Utilisateurs
								</a>
								<ul class="dropdown-menu" aria-labelledby="navbarDropdown">
									<li>
										<a class="dropdown-item" href="{{ path('user_list') }}">
											<i class="fas fa-list me-2"></i>Liste des utilisateurs
										</a>
									</li>
									<li>
										<a class="dropdown-item" href="{{ path('user_create') }}">
											<i class="fas fa-user-plus me-2"></i>Créer un utilisateur
										</a>
									</li>
								</ul>
							</li>
						{% endif %}
						{% if app.user %}
							<li class="nav-item">
								<a href="{{ path('logout') }}" class="nav-link btn-custom" style="background-color: #ff3b30;">
									<i class="fas fa-sign-out-alt me-2"></i>Se déconnecter
								</a>
							</li>
						{% endif %}
						{% if not app.user and 'login' != app.request.attributes.get('_route') %}
							<li class="nav-item">
								<a href="{{ path('login') }}" class="nav-link btn-custom" style="background-color: #34c759;">
									<i class="fas fa-sign-in-alt me-2"></i>Se connecter
								</a>
							</li>
						{% endif %}
					</ul>
				</div>
			</div>
		</nav>

		<main class="flex-grow-1 py-5">
			<div class="container">
				{% for label, messages in app.flashes %}
					{% for message in messages %}
						<div class="alert alert-{{ label }} alert-dismissible fade show shadow-sm mb-4" role="alert">
							<div class="d-flex align-items-center">
								<i class="fas fa-{{ label == 'success' ? 'check-circle' : 'exclamation-circle' }} me-2 fs-4"></i>
								<div>
									<strong>{{ label == 'success' ? 'Action réalisée !' : 'Oops !' }}</strong>
									{{ message }}
								</div>
							</div>
							<button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
						</div>
					{% endfor %}
				{% endfor %}

				<div class="row">
					<div class="col-12 mb-4"> {% block header_title %}{% endblock %}
						</div>
					</div>

					<div class="row">
						<div class="col-12 mb-4">
							{% block header_img %}
								<img class="img-fluid rounded shadow-sm w-100" src="{{ asset('img/todolist_homepage.jpg') }}" alt="todo list">
							{% endblock %}
						</div>
					</div>

					<div class="row">
						<div class="col-12">
							<div class="bg-white p-4 rounded shadow-sm"> {% block body %}{% endblock %}
								</div>
							</div>
						</div>
					</div>
				</main>

				<footer class="footer py-3">
					<div class="container text-center">
						<span class="text-muted">&copy;
							{{ 'now'|date('Y') }}
							OpenClassrooms. Tous droits réservés.</span>
					</div>
				</footer>

				<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
				<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
			</body>
		</html>
